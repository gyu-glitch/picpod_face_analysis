# 분석 서버 새 PC 세팅

## 필요 사양

- Windows 10/11
- **GPU 권장** — 7.8B 모델은 VRAM 6GB 이상. GPU가 없거나 작으면 2.4B 선택
  (CPU만으로도 동작하지만 페르소나 1개당 1분 이상 걸릴 수 있음)
- 디스크 여유 15GB 이상 (모델 7.8B 4.8GB + 2.4B 1.6GB + 패키지)

## 순서

1. **레포 받기**
   - GitHub Desktop으로 `gyu_pickpod` 클론 → `C:\picpod_portrait`
   - 또는 기존 현장세팅.bat 사용

2. **`face-analysis\서버설치.bat` 더블클릭**
   - Python·Ollama 자동 설치, venv 구성, 모델 다운로드(선택), 바탕화면 바로가기 생성
   - Python이 없던 PC라면 설치 후 **한 번 더 실행**해야 함 (PATH 갱신 때문)
   - 모델 다운로드에 수 분~십수 분 소요

3. **`방화벽허용.bat` 우클릭 → 관리자 권한으로 실행** (최초 1회)
   - 포트 8123을 내부망에 열어줌. 이걸 안 하면 다른 PC에서 접속 불가

4. **바탕화면 [PicPod Analysis Server] 실행**
   - 창이 뜨고 서버 주소가 표시됨. **이 창을 닫으면 서버가 꺼짐**

5. **클라이언트 PC 설정 변경**
   - 각 클라이언트의 `config.json`에서 `server` 주소를 새 PC의 IP로 수정
   - 맥/윈도우 클라이언트 zip은 `picpod_client_win.zip`, `picpod_client_mac.zip`

## 확인

브라우저에서 `http://localhost:8123` → 테스트 GUI가 뜨면 정상.
샘플 사진이 없으면 드롭다운이 비어 있는데, 직접 업로드로 테스트하면 된다.

## git으로 안 넘어가는 것들 (서버설치.bat이 처리)

| 항목 | 크기 | 처리 |
|---|---|---|
| `.venv` | ~700MB | requirements.txt로 재생성 |
| Ollama + EXAONE | 1.6~6.4GB | winget + ollama pull |
| `models/face_landmarker.task` | ~3MB | 자동 다운로드 |
| `tests/faces` | — | 테스트 샘플, 없어도 동작 |
| `results/` | — | 실행하며 쌓임 |

## 자동 시작 (선택)

PC 재부팅 후에도 서버가 뜨게 하려면 `서버실행.bat` 바로가기를 시작 프로그램 폴더에
넣는다. `Win+R` → `shell:startup` → 바로가기 복사.
