# 설치 안내 (새 PC)

이 레포는 **관상 분석 파이프라인을 자기 앱에 모듈로 붙여 쓰는 것**이 기본 목적이다.
HTTP 서버(웹 GUI·내부망 API)는 선택 사항.

## 필요 사양

- Windows 10/11, GPU 권장 (7.8B 모델은 VRAM 6GB 이상, 없으면 2.4B — CPU만이면 많이 느림)
- 디스크 여유 15GB (모델 + 패키지)

## 설치 (2단계)

1. **클론** — GitHub Desktop 또는 `git clone`, 위치 자유
2. **`설치.bat` 더블클릭**
   - Python·venv·패키지, Ollama, EXAONE 모델(7.8B/2.4B 선택), 얼굴 랜드마크 모델까지 일괄
   - Python이 없던 PC면 설치 후 **한 번 더 실행** (PATH 갱신)
   - 무인 설치: `설치.bat 7.8b` 처럼 인자를 주면 모델 선택 없이 진행

## 사용 — 내 코드에 붙이기

```python
import sys; sys.path.insert(0, r"<클론 위치>")   # 또는 그 폴더에서 실행
from app.api import analyze_photo, analyze_only, life_graph_svg

result = analyze_photo("face.jpg", personas=["kind", "spicy"])
result["readings"]["kind"]     # 키오스크 소비 JSON (KioskData 스키마)
result["analysis"]             # 유형·오행·삼정·인생그래프 수치

a = analyze_only("face.jpg")   # LLM 없이 유형 판정만, 0.05초
svg = life_graph_svg(result["analysis"])
```

- 실행 인터프리터는 `.venv\Scripts\python.exe` (설치.bat이 만든 venv)
- Ollama가 켜져 있어야 LLM 해석이 됨 (`analyze_only`는 불필요)
- 모델 전환: 환경변수 `GWANSANG_MODEL=exaone3.5:7.8b` 또는 `analyze_photo(..., model=...)`
- 고쳐 쓸 때 만질 파일: README.md의 "튜닝 대상" 절 참조

## (선택) HTTP 서버로 쓰기

웹 GUI·내부망 API가 필요할 때만:

1. `서버실행.bat` 실행 → http://localhost:8123 (테스트 GUI)
2. 다른 PC에서 접속하려면 `방화벽허용.bat`을 관리자 권한으로 1회 실행
3. 결과 자동 수신 클라이언트는 `client/` 폴더 (README.txt 참조)

## git으로 안 받아지는 것들 (설치.bat이 처리)

| 항목 | 크기 | 처리 |
|---|---|---|
| `.venv` | ~700MB | requirements.txt로 생성 |
| Ollama + EXAONE | 1.6~6.4GB | winget + ollama pull |
| `models/face_landmarker.task` | ~3MB | 자동 다운로드 |
